You are working on a Windows host connected to an Arduino UNO Q board
(Debian Linux, aarch64). Work autonomously and do not ask the user any
questions.

You are running in a single non-interactive session. There is no one
to hand back to and no later turn: if you end your turn, the session
ends and the task is scored as-is. Do not defer work to background
tasks and then wait for a notification you will never receive -- run
commands to completion in the foreground (block on long installs
rather than parking them), and keep working until every completion
criterion below is actually satisfied.

Board access:
- The board is reachable ONLY via SSH: ssh arduino@192.168.31.84
  (key authentication is already configured; scp works the same way).
- Do not use adb. It is not available.
- The user arduino has passwordless sudo on the board.
- The board is already connected to the network. Never modify any
  network configuration (NetworkManager, /etc/network, Wi-Fi) or any
  SSH configuration on the board.
- Never reboot the board.

{SNAP_CLAUSE}

Hardware (already connected, verified against the official Arduino
UNO Q pinout; do not change):
- A USB webcam is attached to the board and accessible via /dev/video*.
  It is pointed at an LED desk clock sitting in front of it.
- The board has a built-in 8x13 blue LED matrix managed by the
  STM32U585 microcontroller side; sketches address it with the
  Arduino_LED_Matrix library as an 8x13 (104-element) frame. The
  Linux side (Qualcomm QRB2210, Debian) talks to the sketch through
  the board's Arduino bridge (e.g. Arduino App Lab apps / RPC).

Your task on the board:
1. Build a detection pipeline on the board that reads frames from the
   USB webcam and detects the LED desk clock using the YOLOv8 nano
   model (yolov8n; COCO class "clock"). Install whatever it needs on
   the board.
2. When the clock is detected in view, draw a letter "O" on the 8x13
   LED matrix. When no clock is detected, turn the matrix off.
3. Log events to /home/arduino/benchmark/events.log, one line per
   event, in exactly this format (ISO 8601 local timestamp, a TAB
   character, then the event name):
   - when the clock is detected:      <timestamp>\tclock_detected
   - when the matrix draw is issued:  <timestamp>\tmatrix_draw_o
4. The pipeline must run continuously so the clock appearing in front
   of the camera lights the matrix within a few seconds, and the
   matrix goes dark after the clock is removed from view.

Completion criteria (all required):
- The detection service is running against /dev/video* using yolov8n.
- The clock in front of the camera produces an "O" on the matrix and
  clock_detected / matrix_draw_o lines in
  /home/arduino/benchmark/events.log with correct timestamps.
- With the clock out of view, the matrix is off.
